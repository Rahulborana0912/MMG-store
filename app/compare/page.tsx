import React from 'react';
import CompareClient from '@/components/CompareClient';

export const metadata = {
  title: 'Compare Stone Slabs & Tiles | MMG Mahadev Marble and Granite',
  description: 'Compare dimensions, finishes, square foot rates, and thicknesses of natural marble and granite slabs side-by-side.',
};

export default function ComparePage() {
  return <CompareClient />;
}
